---
title: GPU Acceleration
alphabet: G
tags: [gpu, os]
---

The use of a computer's graphics processing unit (GPU) to speed up rendering and computational tasks. Ghostty leverages GPU acceleration for fast text rendering on macOS (Metal) and Linux (OpenGL).
